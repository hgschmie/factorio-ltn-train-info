------------------------------------------------------------------------
-- styles
------------------------------------------------------------------------

-- local const = require('lib.constants')

-- local styles = data.raw['gui-style'].default

-- styles[const:with_prefix('gui-frame')] = {
--     type = 'frame_style',
--     parent = 'container_invisible_frame_with_title',
--     top_padding = 10,
-- }

-- styles[const:with_prefix('delivery-settings-table')] = {
--     type = 'table_style',
--     vertical_align = 'center',
-- }

-- styles[const:with_prefix('delivery-settings-radiobutton-flow')] = {
--     type = 'vertical_flow_style',
--     vertical_align = 'center',
--     left_padding = 10,
--     right_padding = 10,
-- }

-- styles[const:with_prefix('settings-divide-label')] = {
--     type = 'label_style',
--     parent = 'label',
--     vertical_align = 'center',
--     right_padding = 10,
-- }

-- styles[const:with_prefix('divide-flow')] = {
--     type = 'horizontal_flow_style',
--     vertical_align = 'center',
-- }

-- styles[const:with_prefix('divide-slider')] = {
--     type = 'slider_style',
--     minimal_width = 80,
--     horizontally_stretchable = 'off',
--     vertical_align = 'center',
--     right_padding = 5,
-- }

-- styles[const:with_prefix('divide-text')] = {
--     type = 'textbox_style',
--     vertical_align = 'center',
--     minimal_width = 40,
--     maximal_width = 40,
--     horizontally_stretchable = 'off',
-- }
